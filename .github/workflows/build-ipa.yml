name: Build IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate project
        run: xcodegen generate

      - name: Build app
        run: |
          xcodebuild \
            -project VTeenWebView.xcodeproj \
            -scheme VTeenWebView \
            -sdk iphoneos \
            -configuration Release \
            CONFIGURATION_BUILD_DIR=$PWD/build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Package IPA (FINAL)
        run: |
          rm -rf Payload VTeenWebView.ipa
          mkdir Payload
          cp -R build/VTeenWebView.app Payload/

          # dùng ditto chuẩn Apple (QUAN TRỌNG)
          ditto -c -k --sequesterRsrc --keepParent Payload VTeenWebView.ipa

      - name: Check IPA size
        run: |
          echo "=== IPA SIZE ==="
          ls -lh VTeenWebView.ipa

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: VTeenWebView-ipa
          path: VTeenWebView.ipa
